// Code your solutions in this file
const message = [];
function writeCards(name) {
  for (let c = 0; c < name.length; c++) {
    message.push(`Thank you, ${name[c]}, for the wonderful surprise gift!`);
  }
  return message;
}
writeCards[message];

function countDown(number) {
    let count = 11;
    while (count > 0) {
        console.log(count-- -1);
    }
}
console.log(count-- -1);